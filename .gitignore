# Never commit raw uncompressed logs (too big for GitHub). Commit the .gz instead.
*.txt
logs/*.txt
!*.txt.gz

# Python bytecode cache — auto-generated, machine-specific, disposable.
__pycache__/
*.pyc
